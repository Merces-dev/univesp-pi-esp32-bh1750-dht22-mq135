#pragma once

// ─── Credenciais Wi-Fi ───────────────────────────────────────────────────────────────────
// Este arquivo está no .gitignore — NÃO versionar com dados reais.
#define WIFI_SSID ""
#define WIFI_PASS ""

// ─── Broker MQTT ───────────────────────────────────────────────────────────────────────
// Para usar o broker local Docker: use o IP da máquina na rede (ex: 192.168.x.x)
// Para usar público: broker.hivemq.com
#define MQTT_BROKER  ""     // << troque pelo IP do seu PC na rede
#define MQTT_PORT    1883
#define MQTT_USER    ""                   // vazio = sem autenticacão
#define MQTT_PASS_   ""                   // sufixo _ evita conflito com WIFI_PASS
#define MQTT_CLIENT  "esp32-bh1750-001"   // ID único por dispositivo
